export enum tagTypes {
    admin = "admin",
    user = "user",
    flat = "flat",
    flatShareRequest = "FlatShareRequest",

  }
  
  export const tagTypesList = [
    tagTypes.admin,
    tagTypes.user,
    tagTypes.flat,
    tagTypes.flatShareRequest,
  ];
  